/** 全局常量 */
export const MAX_DIMENSION = 1024    // 图片压缩上限（px）
export const MAX_FILE_MB = 10        // 单文件大小上限（MB）
export const CAPTURE_HOLD_MS = 1500  // 摄像头角度保持时长（ms）自动触发
export const DEFECT_CONFIDENCE_MIN = 0.7  // 低于此 confidence 不渲染引导折线
